# The divolte-env.sh will be sourced by the startup script prior to starting the JVM.

# Uncomment and use this to set a alternative JAVA_HOME
# JAVA_HOME="/path/to/jdk8"

# Uncomment to add specific JVM options; by default Divolte Collector will use "-XX:+UseG1GC -Djava.awt.headless=true"
# DIVOLTE_JAVA_OPTS="-XX:+UseG1GC -Djava.awt.headless=true"
