divolte.tracking.schema_mapping {
  version = 1
  
  fields {
    // Mappings for custom cookies require the cookie name.
    // We should detect if that's missing during startup.
    customCookie = {
      type = cookie
    }
  }
}
