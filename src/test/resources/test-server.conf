include classpath("reference.conf")

divolte {
  server.host = 127.0.0.1

  incoming_request_processor {
    threads = 1
    max_write_queue = 10
    max_enqueue_delay = 1 second
  }

  kafka_flusher.enabled = false
  hdfs_flusher.enabled = false
}
