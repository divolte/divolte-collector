divolte.tracking.schema_mapping {
  version = 1
  
  fields {
    sessionStart = firstInSession
    ts = timestamp
    location = location
    referer = referer
    remoteHost = remoteHost
    userAgentString = userAgent
    client = partyId
    session = sessionId
    eventType = eventType
    pageview = pageViewId
    viewportWidth = viewportPixelWidth
    viewportHeight = viewportPixelHeight
    screenWidth = screenPixelWidth
    screenHeight = screenPixelHeight
    pixelRatio = devicePixelRatio
  }
}
